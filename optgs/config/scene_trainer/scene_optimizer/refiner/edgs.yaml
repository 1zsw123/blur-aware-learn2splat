name: edgs

do_densify: false
do_prune: true
do_opacity_reset: false

#
cap_max: -1  # no cap

# scheduling
pause_refine_after_reset: 0
refine_every: 100
reset_every: 1000000  # effectively disable
refine_start_iter: 500
refine_stop_iter: 15000
refine_scale2d_stop_iter: 0

# thresholds
grow_grad2d: 0.0  # disabled
grow_scale3d: 0.0  #  disabled
prune_scale3d: 0.0  # disabled
prune_scale2d: 0.0  # disabled
grow_scale2d: 0.0  # disabled
min_opacity: 0.005

# Pruning parameters
prune_zero_radii: false

# Slightly reduce opacity every few steps
reduce_opacity: true
reduce_factor: 0.99
reduce_every: 10

fallback_means_lr: 0.0  # unused (noise_lr=0.0)